<h3>Grid</h3>

@code{
    [Parameter] public List<List<Tile>> Tiles { get; set; }
}

<table>
    <tbody>
        @for (int i = 0; i < Tiles.Count; i++) // Rows
        {
            <tr>
                @for (int j = 0; j < Tiles[0].Count; i++) // Columns
                {
                    <td>
                        Test
                    </td>
                }
                </tr>
            }
    </tbody>
</table>
